name "紅石訊號"

-- 每接收到 1 次紅石脈衝訊號，就執行一輪程式
every redstone pulse do
    input from a
    output to b
end

every 20 ticks do
    -- 這會檢查管理器本身的紅石訊號強度
    if redstone ge 2 then
        input from a
        output to b
    end
end
